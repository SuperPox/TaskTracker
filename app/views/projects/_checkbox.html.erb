
<%=form_for(@project) do |f|%>
    <%=f.form_for :tasks do |t| %>  
        <%= t.check_box_tag(:task_status) %>
        <%= t.label_tag(:task_status, "Task Status") %>
    <%end%>
  <%=f.submit "Submit"%>
<%end%>
